package Day_1;
/*import java.util.*;
public class LinearSearch {
    public static void main(String[] args) {
        Aadi[] aadi = new Aadi[10];

        for (int i = 0; i < aadi.length; i++) {
            aadi[i]=new Aadi();
            aadi[i].setName("AAdi"+i);
            aadi[i].setMarks(20+i);
        }

        System.out.println(Arrays.toString(aadi));

       int value = 20;
        int pos = -1;
        pos = getPos(aadi, value);
        if (pos != -1) {

            System.out.println("Element found at "+ pos +" index");
        } else {
            System.out.println("Element not found");
        }

    }

    public static int getPos(Aadi aadi, int value) {
        int pos=-1;
        for (int i = 0; i < aadi.length; i++) {
            if (aadi[i].getMarks== value) {
                pos = i;
                break;
            }
        }
        return pos;
    }
}

class Aadi{
    private String name;
    private int marks;


    public String getName() {
        return name;
    }

    public int getMarks() {
        return marks;
    }

    public void setName(String name) {
        this.name = name;
    }

    public void setMarks(int marks) {
        this.marks = marks;
    }

    @Override
    public String toString() {
        return ((name +" "+marks));
    }
}
*/